Python script to assist in translating ISO 19139 geospatial metadata to the JSON solr schema implimented by the Geoblacklight project.

The script assumes that the metadata exists in ISO 19139 and that metadata has already been built out for all datasets.
